import { Flex } from 'theme-ui';

export default function VotePage() {
  return (
    <Flex
      sx={{
        flex: 1,
        flexDirection: 'column',
      }}
    >
      VotePage
    </Flex>
  );
}
