import { Flex } from 'theme-ui';

export default function PoolPage() {
  return (
    <Flex
      sx={{
        flex: 1,
        flexDirection: 'column',
      }}
    >
      PoolPage
    </Flex>
  );
}
