import { Flex } from 'theme-ui';

export default function SwapPage() {
  return (
    <Flex
      sx={{
        flex: 1,
        flexDirection: 'column',
      }}
    >
      SwapPage
    </Flex>
  );
}
